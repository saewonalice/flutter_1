import 'package:flutter/material.dart';

class AccountUsed extends StatefulWidget {
  const AccountUsed({Key? key}) : super(key: key);

  @override
  State<AccountUsed> createState() => _AccountUseState();
}

class _AccountUseState extends State<AccountUsed> {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,

    );
  }
}
